# Step 1: Init

This requires running a few commands in separate sub steps. Remember, only do one at a time, commit, push, exit. The next one is handled in another prompt.

## Substep 1.1:
[ ] Run `./init.sh 1` for the first step.

## Substep 1.2:
[ ] Run `./init.sh 2` for the second step.

## Substep 1.3:
[ ] Run `./init.sh 3` for the third step.

## Substep 1.4:
[ ] Run `./init.sh 4` for the last init step.
